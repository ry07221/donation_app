%p#notice= notice
%h1 Users
%table
  %thead
    %tr
      %th 名前
      %th フリガナ
      %th ニックネーム
      %th{:colspan => "3"}
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.name_reading
        %td= user.nickname
        %td= link_to '詳細', user
        %td= link_to '編集', edit_user_path(user)
        %td= link_to '削除', user, method: :delete, data: { confirm: '本当に削除しますか?' }
        %td= link_to '寄付する', donate_user_path(user)
%br/
= link_to '新規登録', new_user_path
